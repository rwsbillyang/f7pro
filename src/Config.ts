
export const f7ProConfig = {
    EnableLog: false,
    /**
     * 后端在通知前端oauth认证时采用的前端路径必须与此一致，
     * 注意： 前端若是SPA，通知路径可能需要添加browserHistorySeparator
     */
    BrowserHistorySeparator: "#!",
    TextBack: "返回"
}
